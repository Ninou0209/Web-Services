/* IST & CC CLOSSET Marine LENOIR Guillaume */ 
package com.example.carservice;
/*
import java.util.List;
import java.util.ListIterator;

public class CarList {



    private static List<Car> cars;

    public static List<Car> getCars() {
        ListIterator<Car> it = cars.listIterator();
        System.out.println(it.next().getPlateNumber());
        return cars;
    }

    public static void setCars(List<Car> cars) { 
        CarList.cars = cars;
    }
    
    public static Car getCar(String plateNumber) { 
        ListIterator<Car> it = cars.listIterator();
        Car myCar = null;
        Car temp;
        while(it.hasNext()) {
            temp = it.next();
            if(temp.getPlateNumber().equals(plateNumber)) {
                myCar = temp;
                System.out.println(myCar.getPlateNumber());
            }
        }
                
        return myCar;
    }
    
    public static void deleteCar(String plateNumber) { 
        ListIterator<Car> it = cars.listIterator();
        Car myCar = null;
        Car temp;
        while(it.hasNext()) {
            temp = it.next();
            if(temp.getPlateNumber().equals(plateNumber)) {
                myCar = temp;
                System.out.println("delete " + myCar.getPlateNumber());
                it.remove();
            }
        }


	
}*/
